// rrd4j Persistenzen werden für Grafiken genutzt...

Strategies {
	everyMinute     : "0 * * * * ?"
	everyHour       : "0 0 * * * ?"
	everyDay        : "0 0 0 * * ?"
}

// alle Items in der Gruppe graph_persist werden für rrd Grafiken persistiert
Items {
	Temperatur*,persistGraph* : strategy = everyMinute, restoreOnStartup
}