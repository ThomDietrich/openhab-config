// mapdb Persistenzen werden für Einstellungswerte genutzt...
// Es wird nur der letzte aktuelle Wert gespeichert!

Strategies {
	everyMinute	    : "0 * * * * ?"
	everyHour       : "0 0 * * * ?"
	everyDay        : "0 0 0 * * ?"
}

Items {
	persistValue*,gLights* : strategy = everyChange, restoreOnStartup
}
